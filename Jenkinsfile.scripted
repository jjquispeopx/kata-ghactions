#!/usr/bin/env groovy

node {
  checkout scm

  stage('build') {
    echo 'Environment: $ENVIRONMENT'
    echo 'building...'
  }

  stage('test') {
    echo 'testing...'
  }

  stage('release') {
    echo 'pushing...'
  }

  stage('deploy') {
    echo 'deploying...'
  }
}
