#!/usr/bin/env groovy

properties([
  parameters([
    string(name: 'ENVIRONVENT', defaultValue: "TEST", description: "Indicates the current execution environment.")
  ])
])

node {
  checkout scm

  stage('build') {
    echo 'Parameter: ${params.ENVIRONMENT}'
    echo 'building...'
  }

  stage('test') {
    echo 'testing...'
  }

  stage('release') {
    echo 'pushing...'
  }

  stage('deploy') {
    echo 'deploying...'
  }
}
